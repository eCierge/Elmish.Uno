<Page d:DataContext="{d:DesignInstance Type=samples:DesignData, IsDesignTimeCreatable=True}"
  x:Class="Elmish.Uno.Samples.TwoWaySeq.TwoWaySeqPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:samples="using:Elmish.Uno.Samples"
  xmlns:wct="using:CommunityToolkit.WinUI.Controls"
  xmlns:utoolkit="using:Uno.UI.Toolkit"
  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
  mc:Ignorable="d">

  <StackPanel d:DataContext="{Binding TwoWaySeq}" utoolkit:VisibleBoundsPadding.PaddingMask="All">

    <wct:TokenizingTextBox Margin="10" ItemsSource="{Binding Tokens}" />

    <Button
      Width="150"
      HorizontalAlignment="Center"
      VerticalAlignment="Top"
      Command="{Binding AddToken}"
      Content="Add token" />

  </StackPanel>
</Page>
